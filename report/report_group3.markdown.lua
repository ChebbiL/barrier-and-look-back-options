local kpse = require('kpse') kpse.set_program_name('luatex') local lfs = require("lfs") local cacheDir = "./_markdown_report_group3" if lfs.isdir(cacheDir) == true then else assert(lfs.mkdir(cacheDir)) end local md = require("markdown") local convert = md.new({cacheDir = "./_markdown_report_group3", } ) local input = assert(io.open("report_group3.markdown.in","r")):read("*a") print(convert(input:gsub("\r\n?", "\n")))
